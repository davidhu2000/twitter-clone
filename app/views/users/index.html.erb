<%= provide(:title, "All Users") %>

<h1>All Users</h1>

<%= will_paginate %>

<div class="row">
  <div class="col-md-7 col-md-offset-3">
    
    <div class="users list-group">
      <%= render @users %>
      <!-- render the partial with the same name as the class and iterate through each user -->
    </div>
  </div>
</div>

<%= will_paginate %>